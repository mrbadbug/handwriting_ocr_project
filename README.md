# Img-to-txt Converter

This is a simple tool that converts images into text using optical character recognition (OCR).
You provide image files, the tool reads them, and outputs the extracted text.

## Features
- Load images (JPG, PNG, etc)  
- Image preprocessing (grayscale, thresholding)  
- OCR via `pytesseract` for text extraction  
- Save output to a text file  
- Easy to extend for multiple languages or batch processing

## Installation
1. Clone the repository:
   ```bash
   git clone https://github.com/mrbadbug/Img-to-txt-Converter.git
   cd Img-to-txt-Converter

   cd to run-
python -m venv venv
.\venv\Scriptivates\act
uvicorn main:app --reload

Accuracy-
GoogleAPI can be added to get more accuracy.
